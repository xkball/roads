# 功能规划

找不到美工的时候先糊弄个模型, 没有动画什么的

## 单方块机器

- [ ] 打包/解包机, 可以把1~16组的同种物品打包为包裹, 包裹模型从小到大参考DS里S到XL的模型, 包裹可以作为方块放地上.可以开启自动模式, 
- [ ] 自动打包/解包机, 打包时设置打包数量, 前后输出自动输入输出物品/包裹

## 多方块系统

表现类似巨型单方块机器, 放置直接放置完整多方块, 拆除任意方块拆除完整方块, 对应指定了capability的结构方块, 都只起到托管到主方块的功能. 对任意方块都能右键打开gui.

- [ ] 所有方块都是方块实体, 区分主方块和其他结构方块, 主方块有tick, 其他方块没有.
- [ ] 添加两个方块, 分别为假主方块和假结构方块(不对应mc的结构方块!), 用于创建多方块.
- [ ] 添加选区工具,  左键清空右键标点, 标点方式类似于小木斧的extend模式, 选取所有点的最大包围盒, 在客户端用世界边界的方式渲染包围盒
- [ ] 添加创建多方块结构命令, 在完成选区后, 保存选区内结构到数据. (三维数组?), 数据复制到剪贴板, 稍后可以直接粘贴进某多方块主类里. 当然这要求选区内有主方块.
- [ ] 创建一个多方块会创建仅需要创建一个主方块服务端类和一个客户端类, 自动创建对应物品.
- [ ] 多方块物品拿在手上有放置预览.
- [ ] 需要添加的多方块: 发电机(1 * 4 * 1), 瞭望塔(3 * 10 * 3), 滑索(1 * 4 * 1), 寄存桶(1 * 2 * 1)(其实只是大箱子), 路基(3 * 2 * 2) 
- [ ] 如果时间充足考虑空中单轨车

## 道路

模组的核心内容. 目前先考虑单条道路, 交叉口之后设计

- [ ] 路基负责存储控制点, 创建的额外碰撞可能需要存储在区块.
- [ ] 每个路基可以在半径16*16内铺路, 初始五个控制点, 两个额外控制点, 两个沿路基朝向前后延伸的控制点, 一个始终通过路基本身不能移动的控制点.

- [ ] 使用首尾相接的三阶HERMITE样条曲线, 通过每端两个额外控制点可以创建经过每个控制点的平滑曲线, 可以通过将额外控制点设为下一条路的起始控制点来连接道路.
- [ ] 道路需要任意形状的碰撞 碰撞的实现未决定, 可能方案: 1.使用铁砧库的碰撞, 其能根据给给定大量三角形和指定动量, 算出在每个轴上最大可移动量. 2. http://github.com/Polari-Stars-MC/Rapier-Native 有人想往踢炕添加现成物理库. 如果使用此方案则可以做车辆.
- [ ] 添加物品, 在世界内移动控制点
- [ ] 添加在x3dmap内规划和移动控制点的功能.实际上路基打开gui应该就是x3dmap的界面.